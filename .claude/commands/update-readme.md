---
description: Regenerate README.md from ecosystem.json
---

Run `python3 scripts/generate_readme.py` from the repo root. Report the output (project count + segment count). If the script fails, diagnose and fix.
