---
description: Validate graph/nodes.json + graph/edges.json integrity
---

Run `python3 scripts/validate_graph.py` from the repo root. Report the output verbatim. If there are errors, diagnose and fix (or ask the user if the fix is ambiguous). If there are only warnings, surface them briefly and continue.

Pass `--strict-descriptions` if the user asks for strict mode (`/validate strict`).

$ARGUMENTS
